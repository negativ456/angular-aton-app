export type LoginForm = {
  email: string;
  password: string;
};
export type RegistrationForm = {
  email: string;
  password: string;
};

export interface UserData {
  email: string;
  password: string;
}
